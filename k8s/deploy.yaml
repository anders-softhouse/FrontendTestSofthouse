apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: fe-test
  name: test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fe-test
  template:
    metadata:
      labels:
        app: fe-test
    spec:
      containers:
      - name: main
        image: europe-north1-docker.pkg.dev/code-test-stockholm/code-test/person-family:1.3
        resources:
          requests:
            memory: "250Mi"
            cpu: "250m"
          limits:
            memory: "250Mi"
            cpu: "250m"